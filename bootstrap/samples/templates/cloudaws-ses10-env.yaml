{{- if .Values.samples.cloudaws.ses10_env.enabled }}
---
# CloudAWS for ses10-env — acme-corp AWS environment (for ocp-ses10)
# Apply manually to services cluster: oc apply -f config/samples/ses10-env.yaml
# The operator creates AWSHelper type:environmentprep on the central cluster.
apiVersion: hybridsovereign.redhat/v1alpha1
kind: CloudAWS
metadata:
  name: ses10-env
  namespace: entity-acme-corp
spec:
  account: "390403869973"
  vaultPath: "aws/accounts/shc_admin"
  baseDomain: "sandbox5530.opentlc.com"
{{- end }}
