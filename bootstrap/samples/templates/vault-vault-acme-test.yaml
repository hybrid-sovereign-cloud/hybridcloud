{{- if .Values.samples.vault.vault_acme_test.enabled }}
---
# Test Vault CR for Phase 6 lifecycle validation
# Tests: CR creation, status reconciling→ready, RBAC group mapping, delete cleanup
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Vault
metadata:
  name: acme-test-vault
  namespace: entity-acme-corp
spec:
  ha: false
  rbacConfig: keycloak-sovereign-tenants-services
{{- end }}
