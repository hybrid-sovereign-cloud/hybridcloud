{{- if .Values.samples.rbac.rbac_acme_devops.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-devops
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: "DevOps engineers for Acme Corp -- OpenStack project admin, cluster-admin RBAC inside assigned clusters"
{{- end }}
