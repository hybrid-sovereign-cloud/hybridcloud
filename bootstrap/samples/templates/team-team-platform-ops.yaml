{{- if .Values.samples.team.team_platform_ops.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Team
metadata:
  name: platform-ops
  namespace: entity-acme-corp
spec:
  rbacConfig: rhbk-services
  features:
    argo: false
    istio: false
  teamAdmin:
    - acme-operators
{{- end }}
