{{- if .Values.samples.team.team_frontend.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Team
metadata:
  name: frontend-team
  namespace: entity-acme-corp
spec:
  rbacConfig: rhbk-services
  features:
    argo: false
    istio: true
  teamAdmin:
    - acme-developers
{{- end }}
