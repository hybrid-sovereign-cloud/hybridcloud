{{- if .Values.samples.rbac.rbac_acme_vault_viewers.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-vault-viewers
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: "Vault read-only access for Acme Corp -- view Vault KV and secret paths"
{{- end }}
