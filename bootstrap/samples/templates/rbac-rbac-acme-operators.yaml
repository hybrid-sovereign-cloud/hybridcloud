{{- if .Values.samples.rbac.rbac_acme_operators.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-operators
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: "Cluster operators for Acme Corp -- cluster-operator role inside assigned clusters, vault-ops access"
{{- end }}
