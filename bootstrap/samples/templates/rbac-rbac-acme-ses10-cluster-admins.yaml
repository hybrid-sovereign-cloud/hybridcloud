{{- if .Values.samples.rbac.rbac_acme_ses10_cluster_admins.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-ses10-cluster-admins
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: "Cluster-admin access on ocp-ses10 (AWS) for Acme Corp"
{{- end }}
