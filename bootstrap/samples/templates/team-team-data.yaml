{{- if .Values.samples.team.team_data.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Team
metadata:
  name: data-team
  namespace: entity-acme-corp
spec:
  rbacConfig: rhbk-services
  features:
    argo: true
    istio: false
  teamAdmin:
    - acme-developers
{{- end }}
