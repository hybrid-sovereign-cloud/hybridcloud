{{- if .Values.samples.project.project_api_services.enabled }}
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Project
metadata:
  name: api-services
  namespace: entity-acme-corp
spec: {}
{{- end }}
