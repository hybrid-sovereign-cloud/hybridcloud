---
vault_secret_name: vault-init-secrets
vault_namespace: central-vault
vault_addr: "http://vault-active.central-vault.svc:8200"
vault_kv_path: central
amq_producer_vault_key: amq-producer
amq_consumer_vault_key: amq-consumer
kafka_namespace: amq-streams
kafka_cluster_name: hybridsovereign-kafka
kafka_producer_user: hybridsovereign-producer
kafka_consumer_user: hybridsovereign-eda-consumer
kafka_topic: hybridsovereign-events
kafka_external_bootstrap: "{{ lookup('env', 'KAFKA_EXTERNAL_BOOTSTRAP') | default('', true) }}"
