apiVersion: v2
name: vault
description: HashiCorp Vault in HA mode via Helm subchart
type: application
version: 0.6.11
appVersion: "1.21"
dependencies:
  - name: vault
    version: "0.32.0"
    repository: "https://helm.releases.hashicorp.com"
