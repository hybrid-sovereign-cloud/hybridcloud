# Lab DNS forwarder — real zone/IPs from values-lab / central values lab.*
servers: []
# Example (set via parent chart values):
# servers:
#   - name: lab-domain-forwarder
#     zones: ["lab.example.invalid"]
#     policy: Sequential
#     upstreams: ["127.0.0.1"]
