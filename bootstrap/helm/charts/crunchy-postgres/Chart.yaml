apiVersion: v2
name: crunchy-postgres
description: Crunchy Postgres for Kubernetes (PGO) — upstream Helm chart
type: application
version: 0.5.0
appVersion: "5.8.7"
dependencies:
  - name: pgo
    version: "5.8.7"
    repository: "oci://registry.developers.crunchydata.com/crunchydata"
