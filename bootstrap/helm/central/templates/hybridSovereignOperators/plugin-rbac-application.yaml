{{- if and (include "sovereign-central.applicationsEnabled" .) .Values.pluginRbac.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plugin-rbac
  namespace: {{ .Values.argocd.namespace }}
  labels:
    {{- include "sovereign-central.labels" . | nindent 4 }}
    app.kubernetes.io/component: operator
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.pluginRbac.syncWave | quote }}
spec:
  project: services

  source:
    repoURL: {{ printf "oci://%s/%s/plugin-rbac" .Values.oci.registry .Values.oci.repositoryBase | quote }}
    chart: plugin-rbac
    targetRevision: {{ .Values.pluginRbac.chartVersion | quote }}
    helm:
      releaseName: plugin-rbac
      values: |
        {{- toYaml .Values.pluginRbac.values | nindent 8 }}

  destination:
    server: {{ .Values.servicesCluster.server | quote }}
    namespace: {{ .Values.pluginRbac.destinationNamespace }}

  ignoreDifferences:
    - group: ""
      kind: Namespace
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: ""
      kind: ServiceAccount
      jsonPointers:
        - /imagePullSecrets
        - /secrets
    {{- include "sovereign-central.esoIgnoreDifferences" . | nindent 4 }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
{{- end }}
