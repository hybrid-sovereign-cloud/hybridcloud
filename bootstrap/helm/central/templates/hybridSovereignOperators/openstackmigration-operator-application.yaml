{{- if and (include "sovereign-central.applicationsEnabled" .) .Values.openstackMigrationOperator.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openstackmigration-operator
  namespace: {{ .Values.argocd.namespace }}
  labels:
    {{- include "sovereign-central.labels" . | nindent 4 }}
    app.kubernetes.io/component: operator
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.openstackMigrationOperator.syncWave | quote }}
spec:
  project: services

  source:
    repoURL: {{ printf "oci://%s/%s/openstackmigration-operator" .Values.oci.registry .Values.oci.repositoryBase | quote }}
    chart: openstackmigration-operator
    targetRevision: {{ .Values.openstackMigrationOperator.chartVersion | quote }}
    helm:
      releaseName: openstackmigration-operator
      values: |
        {{- toYaml .Values.openstackMigrationOperator.values | nindent 8 }}

  destination:
    server: {{ .Values.servicesCluster.server | quote }}
    namespace: {{ .Values.openstackMigrationOperator.destinationNamespace }}

  ignoreDifferences:
    - group: ""
      kind: ServiceAccount
      jsonPointers:
        - /imagePullSecrets
        - /secrets
    - group: ""
      kind: Namespace
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    {{- include "sovereign-central.esoIgnoreDifferences" . | nindent 4 }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
{{- end }}
