{{- if and (include "sovereign-central.applicationsEnabled" .) .Values.sovereignDashboard.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sovereign-cloud-dashboard
  namespace: {{ .Values.argocd.namespace }}
  labels:
    {{- include "sovereign-central.labels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.sovereignDashboard.syncWave | quote }}
spec:
  project: services

  source:
    repoURL: {{ printf "oci://%s/%s/sovereign-cloud-dashboard" .Values.oci.registry .Values.oci.repositoryBase | quote }}
    chart: sovereign-cloud-dashboard
    targetRevision: {{ .Values.sovereignDashboard.chartVersion | quote }}
    helm:
      releaseName: sovereign-cloud-dashboard
      values: |
        {{- toYaml .Values.sovereignDashboard.values | nindent 8 }}

  destination:
    server: {{ .Values.servicesCluster.server | quote }}
    namespace: {{ .Values.sovereignDashboard.destinationNamespace }}

  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
    - group: ""
      kind: Namespace
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    {{- include "sovereign-central.esoIgnoreDifferences" . | nindent 4 }}
    - group: oauth.openshift.io
      kind: OAuthClient
      jsonPointers:
        - /secret

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
{{- end }}
