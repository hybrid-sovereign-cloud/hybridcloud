{{- if and (include "sovereign-central.applicationsEnabled" .) .Values.acs.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: acs-central
  namespace: {{ .Values.argocd.namespace }}
  labels:
    {{- include "sovereign-central.labels" . | nindent 4 }}
    app.kubernetes.io/component: security
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.acs.syncWave | quote }}
spec:
  project: central

  source:
    repoURL: {{ printf "oci://%s/%s/acs" .Values.oci.registry .Values.oci.repositoryBase | quote }}
    chart: acs
    targetRevision: {{ .Values.acs.chartVersion | quote }}
    helm:
      releaseName: acs-central
      values: |
        {{- toYaml .Values.acs.values | nindent 8 }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.acs.destinationNamespace }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
{{- end }}
