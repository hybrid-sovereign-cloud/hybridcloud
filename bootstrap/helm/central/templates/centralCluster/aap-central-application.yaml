{{- if and (include "sovereign-central.applicationsEnabled" .) .Values.aapCentral.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aap-central
  namespace: {{ .Values.argocd.namespace }}
  labels:
    {{- include "sovereign-central.labels" . | nindent 4 }}
    app.kubernetes.io/component: automation
    sovereign-cloud/target: central
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.aapCentral.syncWave | quote }}
spec:
  project: central

  source:
    repoURL: {{ printf "oci://%s/%s/aap-instance" .Values.oci.registry .Values.oci.repositoryBase | quote }}
    chart: aap-instance
    targetRevision: {{ .Values.aapCentral.chartVersion | quote }}
    helm:
      releaseName: aap-central
      values: |
        {{- toYaml .Values.aapCentral.values | nindent 8 }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.aapCentral.destinationNamespace }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
{{- end }}
