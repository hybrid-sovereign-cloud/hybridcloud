{{- if and (include "sovereign-central.applicationsEnabled" .) .Values.vaultSecretStoreServices.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-secret-store-services
  namespace: {{ .Values.argocd.namespace }}
  labels:
    {{- include "sovereign-central.labels" . | nindent 4 }}
    app.kubernetes.io/component: secrets
    sovereign-cloud/target: services
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.vaultSecretStoreServices.syncWave | quote }}
spec:
  project: services

  source:
    repoURL: {{ printf "oci://%s/%s/vault-secret-store" .Values.oci.registry .Values.oci.repositoryBase | quote }}
    chart: vault-secret-store
    targetRevision: {{ .Values.vaultSecretStoreServices.chartVersion | quote }}
    helm:
      releaseName: vault-secret-store-services
      values: |
        {{- toYaml .Values.vaultSecretStoreServices.values | nindent 8 }}

  destination:
    server: {{ .Values.servicesCluster.server | quote }}
    namespace: {{ .Values.vaultSecretStoreServices.values.auth.serviceAccountNamespace | default "external-secrets" }}

  ignoreDifferences:
    {{- include "sovereign-central.esoIgnoreDifferences" . | nindent 4 }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
{{- end }}
