---
- name: Validate Operators
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - check_operators
  post_tasks:
    - name: Operators Summary
      ansible.builtin.debug:
        msg: "Operators: {{ 'PASS' if operators_check_passed else 'FAIL' }}"
