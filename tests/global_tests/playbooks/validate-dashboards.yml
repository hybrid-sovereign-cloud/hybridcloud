---
- name: Validate Dashboards
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - check_dashboards
  post_tasks:
    - name: Dashboards Summary
      ansible.builtin.debug:
        msg: "Dashboards: {{ 'PASS' if dashboards_check_passed else 'FAIL' }}"
