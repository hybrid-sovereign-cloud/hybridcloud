---
- name: Validate EDA Events
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - check_eda_events
  post_tasks:
    - name: EDA Summary
      ansible.builtin.debug:
        msg: "EDA: {{ 'PASS' if eda_check_passed else 'FAIL' }}"
