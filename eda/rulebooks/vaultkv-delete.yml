- name: VaultKV Delete Rulebook
  hosts: all
  sources:
  - ansible.eda.event_stream:
      event_stream_name: sovereign-operator-events
  rules:
  - name: Handle VaultKV Delete
    condition: event.payload.reason | default(event.message.reason | default('')) == "VaultKVDeleteRequested" and event.payload.regarding.kind | default(event.message.regarding.kind | default(''))
      == "VaultKV"
    action:
      run_job_template:
        name: vaultkv-teardown
        organization: sovereign
        job_args:
          extra_vars:
            event_payload: '{{ event.payload }}'
