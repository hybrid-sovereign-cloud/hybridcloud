- name: Rbac Create Rulebook
  hosts: all
  sources:
  - ansible.eda.event_stream:
      event_stream_name: sovereign-operator-events
  rules:
  - name: Handle Rbac Create
    condition: event.payload.reason | default(event.message.reason | default('')) in ["RbacCreateRequested", "RbacReconcileRequested"] and event.payload.regarding.kind | default(event.message.regarding.kind | default(''))
      == "Rbac"
    action:
      run_job_template:
        name: rbac-provision
        organization: sovereign
        job_args:
          extra_vars:
            event_payload: '{{ event.payload }}'
