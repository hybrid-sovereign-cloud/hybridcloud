- name: RbacConfig Delete Rulebook
  hosts: all
  sources:
  - ansible.eda.event_stream:
      event_stream_name: sovereign-operator-events
  rules:
  - name: Handle RbacConfig Delete
    condition: event.payload.reason | default(event.message.reason | default('')) == "RbacConfigDeleteRequested" and event.payload.regarding.kind | default(event.message.regarding.kind | default(''))
      == "RbacConfig"
    action:
      run_job_template:
        name: rbacconfig-teardown
        organization: sovereign
        job_args:
          extra_vars:
            event_payload: '{{ event.payload }}'
