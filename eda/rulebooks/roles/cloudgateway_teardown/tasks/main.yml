---
- block:
    - name: Parse event payload
      ansible.builtin.set_fact:
        cloudgateway_cr_name: "{{ event_payload.regarding.name }}"
        cloudgateway_cr_namespace: "{{ event_payload.regarding.namespace }}"

    - name: Resolve services cluster API connection from Vault
      ansible.builtin.include_tasks: "{{ role_path }}/../../../common/tasks/resolve_services_cluster_connection.yml"

    - name: Stub debug teardown for CloudGateway
      ansible.builtin.debug:
        msg: "Stub teardown CloudGateway/{{ cloudgateway_cr_name }}"

    - name: Mark deletion complete
      ansible.builtin.include_tasks: "{{ role_path }}/../../../common/tasks/patch_cr_status.yml"
      vars:
        cr_api_version: hybridsovereign.redhat/v1alpha1
        cr_plural: cloudgateways
        cr_name: "{{ cloudgateway_cr_name }}"
        cr_namespace: "{{ cloudgateway_cr_namespace }}"
        cr_status_body:
          status: ready
          ready: true
          deletionComplete: true
          message: "Stub deleted by EDA hybridvpc"
          lastReconciledAt: "{{ lookup('pipe', 'date -u +%Y-%m-%dT%H:%M:%S.000000Z') }}"

  rescue:
    - name: Mark teardown failed
      ansible.builtin.include_tasks: "{{ role_path }}/../../../common/tasks/patch_cr_status.yml"
      vars:
        cr_api_version: hybridsovereign.redhat/v1alpha1
        cr_plural: cloudgateways
        cr_name: "{{ cloudgateway_cr_name }}"
        cr_namespace: "{{ cloudgateway_cr_namespace }}"
        cr_status_body:
          status: failed
          ready: false
          message: "{{ ansible_failed_result.msg | default('EDA stub teardown failed') }}"
      ignore_errors: true
      when: services_api_host is defined

    - name: Re-raise
      ansible.builtin.fail:
        msg: "CloudGateway teardown stub failed"
