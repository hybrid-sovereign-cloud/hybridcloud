- name: Project Delete Rulebook
  hosts: all
  sources:
  - ansible.eda.event_stream:
      event_stream_name: sovereign-operator-events
  rules:
  - name: Handle Project Delete
    condition: event.payload.reason | default(event.message.reason | default('')) == "ProjectDeleteRequested" and event.payload.regarding.kind | default(event.message.regarding.kind | default(''))
      == "Project"
    action:
      run_job_template:
        name: project-teardown
        organization: sovereign
        job_args:
          extra_vars:
            event_payload: '{{ event.payload }}'
