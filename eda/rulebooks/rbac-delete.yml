- name: Rbac Delete Rulebook
  hosts: all
  sources:
  - ansible.eda.event_stream:
      event_stream_name: sovereign-operator-events
  rules:
  - name: Handle Rbac Delete
    condition: event.payload.reason | default(event.message.reason | default('')) == "RbacDeleteRequested" and event.payload.regarding.kind | default(event.message.regarding.kind | default(''))
      == "Rbac"
    action:
      run_job_template:
        name: rbac-teardown
        organization: sovereign
        job_args:
          extra_vars:
            event_payload: '{{ event.payload }}'
