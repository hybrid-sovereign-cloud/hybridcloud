# Sample VaultKV — acme-test-reinit
# Source: plugin_vault/config/samples/vaultkv-acme-test-reinit.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/vaultkv/acme-test-reinit.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: VaultKV
metadata:
  name: acme-test-reinit
  namespace: entity-acme-corp
spec:
  vault: acme-vault
  vaultAdminRbac:
  - acme-vault-admins
  vaultReaderRbac:
  - acme-developers
