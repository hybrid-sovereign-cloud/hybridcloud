# Sample CloudOSO — ses9-env
# Source: CloudOSO/config/samples/ses9-env.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/cloudoso/ses9-env.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: CloudOSO
metadata:
  name: ses9-env
  namespace: entity-acme-corp
spec:
  vaultPath: oso/accounts/example-admin
  baseDomain: lab.example.com
  projectDomain: example_domain
  externalNetwork: ext-net
  route53VaultPath: oso/accounts/route53-openstack
