# Sample Team — frontend-team
# Source: Team/config/samples/team-frontend.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/team/frontend-team.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Team
metadata:
  name: frontend-team
  namespace: entity-acme-corp
spec:
  rbacConfig: rhbk-services
  features:
    argo: false
    istio: true
  teamAdmin:
  - acme-developers
