# Sample Team — platform-ops
# Source: Team/config/samples/team-platform-ops.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/team/platform-ops.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Team
metadata:
  name: platform-ops
  namespace: entity-acme-corp
spec:
  rbacConfig: rhbk-services
  features:
    argo: false
    istio: false
  teamAdmin:
  - acme-operators
