# Sample Team — data-team
# Source: Team/config/samples/team-data.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/team/data-team.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Team
metadata:
  name: data-team
  namespace: entity-acme-corp
spec:
  rbacConfig: rhbk-services
  features:
    argo: true
    istio: false
  teamAdmin:
  - acme-developers
