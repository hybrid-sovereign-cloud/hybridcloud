# Sample Rbac — acme-ses12-cluster-viewers
# Source: bootstrap/samples/templates/rbac-rbac-acme-ses12-cluster-viewers.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-ses12-cluster-viewers.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-ses12-cluster-viewers
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: Read-only (view) access on ocp-ses12 (AWS) for Acme Corp
