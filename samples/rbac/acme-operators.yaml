# Sample Rbac — acme-operators
# Source: plugin_rbac/config/samples/rbac-acme-operators.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-operators.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-operators
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: Cluster operators for Acme Corp -- cluster-operator role inside assigned
    clusters, vault-ops access
