# Sample Rbac — acme-infra-team
# Source: plugin_rbac/config/samples/rbac-acme-full-layer1.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-infra-team.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-infra-team
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: Infrastructure engineers managing cloud environments and clusters
