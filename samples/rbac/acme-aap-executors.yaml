# Sample Rbac — acme-aap-executors
# Source: plugin_rbac/config/samples/rbac-acme-aap-executors.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-aap-executors.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-aap-executors
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: AAP job executors for Acme Corp -- can launch approved automation jobs
