# Sample Rbac — acme-ses10-cluster-developers
# Source: plugin_rbac/config/samples/rbac-acme-ses10-cluster-developers.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-ses10-cluster-developers.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-ses10-cluster-developers
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: Edit (developer) access on ocp-ses10 (AWS) for Acme Corp
