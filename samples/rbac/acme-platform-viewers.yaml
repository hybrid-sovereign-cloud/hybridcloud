# Sample Rbac — acme-platform-viewers
# Source: plugin_rbac/config/samples/rbac-acme-platform-viewers.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-platform-viewers.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-platform-viewers
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: Platform read-only access for Acme Corp -- view PlatformOpenshift clusters
