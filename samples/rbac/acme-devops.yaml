# Sample Rbac — acme-devops
# Source: plugin_rbac/config/samples/rbac-acme-devops.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/rbac/acme-devops.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Rbac
metadata:
  name: acme-devops
  namespace: entity-acme-corp
spec:
  config: keycloak-sovereign-tenants-services
  description: DevOps engineers for Acme Corp -- OpenStack project admin, cluster-admin
    RBAC inside assigned clusters
