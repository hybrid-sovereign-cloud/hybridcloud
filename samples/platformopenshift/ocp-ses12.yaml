# Sample PlatformOpenshift — ocp-ses12
# Source: PlatformOpenshift/config/samples/ocp-ses12.yaml
# Apply manually to services cluster (NOT via ArgoCD):
#   oc apply -f samples/platformopenshift/ocp-ses12.yaml --context=services-admin
---
apiVersion: hybridsovereign.redhat/v1alpha1
kind: PlatformOpenshift
metadata:
  name: ocp-ses12
  namespace: entity-acme-corp
spec:
  type: openstack
  openstack:
    environment: ses12-env
    controlPlaneCount: 3
    workerCount: 2
    controlPlaneFlavor: b1.xlarge
    workerFlavor: b1.large
    externalNetwork: ext-net
